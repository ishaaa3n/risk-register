# Leave blank for local dev (Vite proxies /api and /uploads to localhost:4000).
# In production, set this to your deployed backend's public URL, e.g.:
# VITE_API_URL=https://risk-register-api.onrender.com
VITE_API_URL=
